#include <stdio.h>

int main()
{

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    int X, Y, multiply;
    scanf("%d%% %d%%", &X, &Y);
    multiply = X * Y;
    // printf("%d\n", X);
    // printf("%d", Y);
    printf("%d%%", multiply);
    return 0;
}